


A nu script to order package update by semantic versioning and to highlight version change on Arch systems.
It uses the command `checkupdates` to fetch updates.

*Important*: It needs the nu binary(nushell) to be installed, then you can run it also in a bash shell but it will appear as csv raw text, not in a table like below.
usage:
```
check_upgrade_type | from csv
```
Output:
![immagine](https://github.com/user-attachments/assets/a538b521-e3ce-482e-96bb-f54cb5855d28)
